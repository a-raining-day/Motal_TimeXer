在 transformer 的位置编码中，如果将位置编码与输入进行拼接，再用一个全局的卷积去卷积，这个卷积核是可学习的
那么会发生什么？
transformer一共两个输入口，因此卷积核数量可以为 1 个完全全局的 和 2 个各自独立的全局的卷积核
卷积核大小为 1 * 1
等价于： w1 * x + w2 * y ( +b)
那么一个卷积核只增加了参数量 2 ~ 3(加上偏置)
一共需要 1 ~ 2个卷积核